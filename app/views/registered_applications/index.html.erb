<h1>Registered Applications</h1>

<%= link_to "Create Registered Application", new_registered_application_path, class: 'btn btn-primary' %>

<ul class="pt20">
	<% @reg_apps.each do |app| %>
		<li>
			<%= link_to app.name, app %>
		</li>
	<% end %>
</ul>
